{
    "buildpacks": [
        {
            "url": "heroku/nodejs"
        }
    ],
    "environments": {
        "test": {
            "scripts": {
                "test": "mocha -r ts-node/register test/**/*.ts"
            }
        }
    }
}